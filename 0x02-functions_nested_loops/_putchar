#include <unistd.h>

/**
*_putchur - write the character c to stdout
*@c: The character to print
*
*Return: ON success 1.
*On error, -1 is returned, and errno is set appropiately.
*/
int_putchar(char c)
{
       return(write(1, &c, 1));
}
